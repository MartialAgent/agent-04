# 📝 과제

원우마다 배경이 달라서 과제도 **트랙별로 나눴습니다.**

| 표시 | 대상 |
|---|---|
| 🟢 | 전원 |
| 🟡 | 전원 (권장) |
| 🔴 | 개발 트랙 |

**🔴을 못 해도 됩니다. 대신 🟢은 전원이 제출합니다.**
바이브코딩(AI에게 시키기)은 **모든 과제에서 적극 권장**합니다. 단, 제출할 땐 **본인 말로 설명할 수 있어야** 합니다.

---

## 🎯 과제 1 — "가장 짧은 트랜스포머"

ChatGPT, Gemini, 클로드... 전부 **트랜스포머(Transformer)** 라는 구조로 만들어진 AI입니다.
3단계([`03-nanogpt/`](03-nanogpt/README.md))에서 본 그 구조를 다룹니다.

### 🟢 전원 — 구조 설명하기

코드를 짜지 않아도 됩니다. **[3단계 학습자료](03-nanogpt/docs/study/01_transformer_architecture.md)를 읽고,
트랜스포머가 문장을 어떻게 처리하는지 그림 하나 + 5~10줄로 설명**하세요.

반드시 포함할 것: **셀프어텐션(self-attention)이 하는 일**을 비유로 한 문장.

### 🔴 개발 트랙 — 직접 구현하기

**가능한 한 짧은 파이썬 코드**로 트랜스포머를 직접 구현해보세요.

- 라이브러리 자유 (PyTorch 추천 — `pip install torch`)
- 길이 기준은 없지만 짧을수록 멋집니다
- **셀프어텐션이 코드 어디에 있는지 주석으로 표시**할 것

힌트 키워드: `self-attention`, `임베딩(embedding)`, `minGPT`, `karpathy` 🔍

### 제출 방법 (공통)

1. 깃허브에 새 리포 만들기 (예: `tiny-transformer`)
2. 결과물 + README(본인 말로 정리) 푸시
3. 리포 주소를 Martial Agent에게 전송

---

## 🎯 과제 2 — 트랜스포머 속 선형대수 🟡

코드를 돌리는 것에서 한 발 더 나가서, 각 연산이 **어떤 선형대수 개념**인지 짚어봅니다.

- [ ] **벡터/임베딩**: 토큰 하나가 왜 숫자 벡터(리스트)로 표현되는지
- [ ] **행렬곱(matmul)**: `Q @ K.T` 한 줄이 왜 "모든 단어 쌍의 유사도"를 한 번에 계산하는지
- [ ] **내적(dot product)**: 두 벡터의 내적이 "얼마나 비슷한 방향인가"를 뜻한다는 것
- [ ] **softmax**: 내적 점수(logit)를 확률(합 1)로 바꾸는 정규화 과정
- [ ] **가중합(weighted sum)**: attention score로 Value 벡터들을 섞는 것 = 가중 평균

짧은 코드 연습:

```python
import numpy as np

# 단어 3개, 차원 4짜리 장난감 예시
Q = np.random.randn(3, 4)
K = np.random.randn(3, 4)
V = np.random.randn(3, 4)

scores = Q @ K.T / np.sqrt(4)          # 행렬곱 → 유사도(내적) 점수
weights = np.exp(scores) / np.exp(scores).sum(axis=-1, keepdims=True)  # softmax
output = weights @ V                    # 가중합 → 새 벡터

print(scores.shape, weights.shape, output.shape)
```

각 줄 옆에 "이건 선형대수의 ○○ 연산이다"를 주석으로 달아서 제출하면 완료.

> 🟢 **선형대수가 처음이라면**: 위 코드를 그냥 실행해서 `shape`가 어떻게 변하는지만 관찰하고,
> "3개의 단어가 서로를 얼마나 참고했는지가 `weights`에 들어 있다"는 문장을 이해하는 것까지가 목표입니다.

---

## 🎯 과제 3 — 내 도메인 에이전트 (학기 과제) 🟢🟡🔴

**한 학기 내내 이어지는 메인 과제입니다.** 5단계([`05-handson/`](05-handson/05-handson.md))에서 시작합니다.

각자 **자기 도메인**의 반복 업무 하나를 골라, 그걸 돕는 에이전트를 만들고 키웁니다.
영업·행정·연구·제조·인사·마케팅 — 무엇이든 좋습니다. **잘 아는 도메인일수록 좋은 과제가 됩니다.**

### 1단계 — 조사 🟢 (코딩 없음)

- [ ] 내 도메인에서 **반복적이고 시간을 많이 잡아먹는 업무** 3가지 목록화
- [ ] 각 업무별: 자동화 가능성 / **어떤 자료를 찾아와야 하는지** / 개인정보 이슈
- [ ] 그중 하나를 골라 "입력 → 에이전트가 할 일 → 결과물" 한 줄로 정리

> 두 번째 항목이 핵심입니다. "우리 조직 데이터를 어디서 가져올 것인가"가 실제 난관입니다.

### 2단계 — 프로토타입 🟡

- [ ] [`05-handson/domain_agent.py`](05-handson/domain_agent.py)의 **분류 항목과 노드 프롬프트**를 내 업무로 교체
- [ ] 실제 예시 입력으로 동작 테스트 (되는 것 / 안 되는 것 기록)
- [ ] 개인정보·민감정보가 들어가면 마스킹·확인 절차 설계

### 3단계 — 확장 🔴

- [ ] 노드 추가 또는 노드 이어붙이기
- [ ] `@tool`로 **내 조직 데이터를 조회하는 도구** 붙이기
- [ ] 되돌릴 수 없는 작업에 사람 승인(`interrupt_before`) 게이트

### 마무리 — 정리 🟢

- [ ] **무엇이 됐고 무엇이 안 됐는지** 정리 (안 된 이유가 더 중요합니다)
- [ ] 결과를 각자 리포의 `notes/` 폴더에 푸시
- [ ] 발표

---

## 🎯 Martial Agent의 과제 — 커리큘럼과 템플릿

원우가 아니라 **강사 쪽 과제**입니다.

- [ ] 04기 원우 도메인 인터뷰 → 도메인별 "자동화 가능 업무" 지도 작성
- [ ] 배경(개발자/준개발자/비개발자)별 **트랙 분리가 실제로 작동하는지** 중간 점검
- [ ] 원우 각자가 복제해 쓸 **리포 템플릿** 정비
- [ ] 위 결과를 활용한 **워크숍용 커리큘럼 초안** + 예제 슬라이드 뼈대

---

## 제출 기한

서로 정한 날짜: ____________ (직접 채워 넣기)
