# 📝 과제

## 🎯 04기의 과제1 — "가장 짧은 트랜스포머"

ChatGPT, Gemini, 클로드... 전부 **트랜스포머(Transformer)** 라는 구조로 만들어진 AI입니다.
이 구조를 **가능한 한 짧은 파이썬 코드**로 직접 구현해보세요.

### 제출 방법
1. 깃허브에 새 리포 만들기 (예: `tiny-transformer`)
2. 코드 + 간단한 README(코드의 각 부분이 뭘 하는지 3~5줄 설명) 푸시
3. 리포 주소를 Martial Agent에게 전송

### 규칙
- **바이브코딩 적극 권장!** AI에게 "가장 짧은 트랜스포머를 파이썬으로 구현해줘"라고 시작해도 됩니다.
- 단, 제출할 땐 **각 줄이 대충 뭘 하는지 설명할 수 있어야** 합니다. (README에 본인 말로 정리)
- 라이브러리는 자유 (PyTorch 추천 — `pip install torch`)
- 길이 기준은 없지만 짧을수록 멋집니다. 셀프어텐션(self-attention)이 코드 어디에 있는지 표시해보세요.

### 힌트가 되는 키워드
`self-attention`, `임베딩(embedding)`, `minGPT`, `karpathy` 🔍

### 04기의 과제2 — 트랜스포머 속 선형대수
코드를 돌리는 것에서 한 발 더 나가서, 각 연산이 **어떤 선형대수 개념**인지 짚어봅니다.

- [ ] **벡터/임베딩**: 토큰 하나가 왜 숫자 벡터(리스트)로 표현되는지
- [ ] **행렬곱(matmul)**: `Q @ K.T` 한 줄이 왜 "모든 단어 쌍의 유사도"를 한 번에 계산하는지
- [ ] **내적(dot product)**: 두 벡터의 내적이 "얼마나 비슷한 방향인가"를 뜻한다는 것
- [ ] **softmax**: 내적 점수(logit)를 확률(합 1)로 바꾸는 정규화 과정
- [ ] **가중합(weighted sum)**: attention score로 Value 벡터들을 섞는 것 = 가중 평균

짧은 코드 연습(선택):
```python
import numpy as np

# 단어 3개, 차원 4짜리 장난감 예시
Q = np.random.randn(3, 4)
K = np.random.randn(3, 4)
V = np.random.randn(3, 4)

scores = Q @ K.T / np.sqrt(4)          # 행렬곱 → 유사도(내적) 점수
weights = np.exp(scores) / np.exp(scores).sum(axis=-1, keepdims=True)  # softmax
output = weights @ V                    # 가중합 → 새 벡터

print(scores.shape, weights.shape, output.shape)
```
각 줄 옆에 "이건 선형대수의 ○○ 연산이다"를 주석으로 달아서 제출하면 완료.

---

## 🎯 Martial Agent의 과제 — "교사/학교용 에이전트 개발 & 교육 콘텐츠 기획"

교사/학교 업무에 **실제로 효과적인 에이전트를 개발**하고, 이를 활용한 **교육 콘텐츠(수업/워크숍용)를 기획**한다.
(기존 "조사" 단계를 넘어 실제 프로토타입 + 콘텐츠 산출물까지 진행)

### 조사 항목 (기반 작업)
- [ ] 반복적이고 시간을 많이 잡아먹는 교사 업무 목록화 (04기 인터뷰 포함)
- [ ] 각 업무별: 자동화 가능성 / 필요한 데이터 / 개인정보 이슈
- [ ] 이미 나와 있는 교육용 AI 도구·사례 조사 (국내외)

### 개발 항목
- [ ] 우선순위 1~2개 업무를 골라 5단계 `teacher_agent.py` 기반 프로토타입 노드로 구현
- [ ] 실제 예시 입력(가정통신문 초안, 공문 등)으로 동작 테스트
- [ ] 개인정보/민감정보가 들어가는 경우 마스킹·확인 절차 설계

### 교육 콘텐츠 기획 항목
- [ ] 위 에이전트를 활용한 수업/워크숍용 커리큘럼 초안 작성 (대상: 교사)
- [ ] 예제 슬라이드·실습 자료 뼈대 잡기
- [ ] 결과를 이 리포에 `notes/` 폴더로 정리해서 푸시

### 우선 살펴볼 업무 후보
가정통신문/안내문, 생기부 문구, 수행평가 채점 보조, 공문 요약, 학부모 소통 문구,
시험 문항 초안, 수업 지도안, 학급 행사 기획, 상담 기록 정리

---

## 제출 기한
서로 정한 날짜: ____________ (직접 채워 넣기)
