/*
  2-2단계 완성본 — 우리 조직 데이터 (전부 더미)
  ================================================================
  ★ 원우가 자기 도메인으로 갈아끼우는 건 사실상 이 파일 하나입니다.

  여기 있는 건 전부 가짜 데이터입니다. 진짜 서비스라면 이 자리에
    - 사내 ERP API 호출
    - Supabase / PostgreSQL 조회
    - 엑셀 파일 읽기
  가 들어갑니다. 어느 쪽이든 **"이름 붙은 것이 값을 돌려준다"** 는 모양은 같습니다.

  그래서 수업에서는 그 모양만 남기고 안을 표로 채웠습니다.
  계정도, 키도, 네트워크도 필요 없이 구조를 그대로 익힐 수 있습니다.

  ★ 옆에 있는 data.py와 내용이 같습니다.
    이 폴더는 같은 것을 두 벌 두었습니다.
      data.js  — 지금 브라우저에서 실제로 돌아가는 것
      data.py  — 진짜 서버로 만들 때의 모습 (읽을거리)
    두 파일을 나란히 열어보세요. 언어만 다르고 하는 일은 똑같습니다.
*/

// ── 더미 사내 데이터 ─────────────────────────────────────────
//
// 예시는 "제조업 재고"입니다. 영업이면 고객사 목록, 행정이면 업무분장표,
// 연구면 서지 목록으로 바꾸면 됩니다. 구조는 그대로 두세요.
//
const INVENTORY = {
  "A-1023": { 이름: "알루미늄 브라켓", 재고: 48, 납기: "3일", 담당: "생산1팀" },
  "B-2041": { 이름: "스테인리스 힌지", 재고: 0,  납기: "3주", 담당: "생산2팀" },
  "C-3077": { 이름: "고무 개스킷",     재고: 5,  납기: "1주", 담당: "생산1팀" },
};


// ── 더미 사용자 ──────────────────────────────────────────────
//
// ⚠️ 이건 인증이 아닙니다. 인증의 "자리"만 만들어둔 것입니다.
//
//    진짜 로그인은
//      비밀번호를 해시로 저장하고, 세션이나 토큰을 발급하고,
//      만료를 관리하고, 비밀번호 재설정 메일을 보냅니다.
//
//    그걸 직접 만드는 대신 Supabase / Google 로그인 같은 서비스를 쓰는 것이고,
//    그게 2-0단계 도구 지도에 그 서비스들이 있는 이유입니다.
//
//    여기서는 "이름만 적으면 통과"입니다. 그래도 배울 건 다 배웁니다.
//      → 사용자가 구분되면 대화방(thread)도 나뉜다
//
const FAKE_USERS = {
  "김영업": { 부서: "영업팀" },
  "박행정": { 부서: "총무팀" },
};

function findUser(name) {
  return { name: name, 부서: (FAKE_USERS[name] || { 부서: "게스트" }).부서 };
}
